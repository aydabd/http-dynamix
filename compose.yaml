services:
  http-dynamix-api-service-tests:
    image: http-dynamix-api-service-tests:latest
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - http-dynamix-tests
networks:
  http-dynamix-tests:
